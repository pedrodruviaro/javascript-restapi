const { Router } = require("express")

const router = Router()

router.get("/:id", () => {})
router.post("/", () => {})
router.put("/:id", () => {})
router.delete("/:id", () => {})

module.exports = router
